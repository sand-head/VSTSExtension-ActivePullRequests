{
  "scripts": {
    "lint": "tslint -c tslint.json 'src/*.ts'",
    "clean": "rimraf ./dist && rimraf ./*.vsix",
    "compile": "npm run lint && npm run clean && webpack --mode production",
    "compile:dev": "npm run lint && npm run clean && webpack --mode development",
    "build": "npm run compile",
    "build:dev": "npm run compile:dev && npm run postbuild",
    "postbuild": "npm run package",
    "package": "tfx extension create --rev-version --manifest-globs ./mimeo-devops-extension.json",
    "gallery-publish": "tfx extension publish --rev-version"
  },
  "devDependencies": {
    "@types/react": "^17.0.27",
    "@types/react-dom": "^17.0.9",
    "base64-inline-loader": "^1.1.1",
    "css-loader": "^2.1.1",
    "mini-css-extract-plugin": "^0.7.0",
    "postcss-loader": "^3.0.0",
    "rimraf": "^3.0.2",
    "sass": "^1.42.1",
    "sass-loader": "^7.3.1",
    "style-loader": "^0.23.1",
    "tfx-cli": "^0.9.3",
    "ts-loader": "^6.1.2",
    "tslint": "^5.20.0",
    "typescript": "^4.4.3",
    "webpack": "^4.41.0",
    "webpack-cli": "^3.3.9"
  },
  "dependencies": {
    "azure-devops-extension-api": "^1.158.0",
    "azure-devops-extension-sdk": "^2.0.11",
    "azure-devops-ui": "^2.167.16",
    "react": "^16.8.1",
    "react-dom": "^16.8.1"
  },
  "name": "vstsexthub",
  "private": true,
  "version": "0.0.0"
}
